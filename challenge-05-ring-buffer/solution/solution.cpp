// Challenge 05: Ring Buffer (SPSC)
// Implementation is header-only (see solution.h) so push()/pop() inline into
// the caller's hot loop. This TU exists only to satisfy the build.
#include "solution.h"
